<%= form_for(current_user.memberships.find_by(user_id: @user.id && group_id: @group.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Leave", class: "btn" %>
<% end %>