<% @users = User.all %>
  <% @users.each do |user| %>
    <div class="glips">
      <div class="post-title"><%= link_to user.username, user %></div>
      <div class="date">Member since <%= time_ago_in_words(user.created_at) %> ago</div>
    </div>
<% end %>
