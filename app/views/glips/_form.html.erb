<div class="log-form">
    
  <%= simple_form_for @glip do |f| %>
  
    <% if @glip.errors.any? %>
      <div class="alert">
        <h2><%= pluralize(@glip.errors.count, "error") %> prohibited this glip from being saved:</h2>
     
        <ul>
        <% @glip.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    
    <%= f.text_field :title, placeholder: 'Title' %><br />
    
    <div class="small">What is it you want to accomplish? Set specific goals that you can measure. "Learning a new language" is a good goal, but it's vague.
    How will you know you've accomplished it? What does it mean to know another language? Think about these things when writing your goals. </div>
    
    <%= f.text_area :content, placeholder: 'Content', rows: 6 %>
    
    <div class="small"><b><div id="markdown"><%= link_to "#", :remote => true do %><i class="fa fa-chevron-down"  aria-hidden="true"></i>&nbsp;Formatting Help<% end %></b></div></div><br />
    
    <div class="small">What is the completion criteria for this glip? How will you know you achieved your goal? Is it verifiable? What is your metric of success for this?</div>
    <%= f.text_area :completion_criteria, placeholder: 'Completion criteria' %>
    
    <div class="small">What is your deadline? (Leave blank if you do not want one.) <em><b>*If the calendar doesn't appear when you click in the field, refresh the page.</em></b></div>
    <%= f.text_field :deadline, id: "datepicker", placeholder: 'Deadline' %>
    
    <%= f.text_field :tag_list, placeholder: 'Add up to 5 tags (separated by commas)', value: @glip.tag_list.join(", ") %>
    <div class="small"><em>Example: fitness, weight loss, marathon</em></div><br />
    
    <%= f.radio_button :privacy, 'shown' %> 
    <%= label :privacy_shown, 'Public' %>
    <%= f.radio_button :privacy, 'hidden' %>
    <%= label :privacy_hidden, 'Private' %>
    <div class="small"><em>Private Glips are visible only to you.</em></div><br />
    
    <fieldset>
      <legend>Image</legend>
        <br />
        <label class="upload-button">Upload Image<%= f.file_field :image, style: 'display:none;' %>
        </label>
        <div class="small"><br/><em>Image should be 620 pixels in width x 300 pixels in height. Must be less than 5Mb.</em></div>
    </fieldset>
    
    <p><%= f.submit %></p>
  <% end %>
</div>
  
