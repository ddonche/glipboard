<div class="log-form">
  <%= simple_form_for(@article, html: { method: :put, multipart: true }) do |f| %>
  
    <% if @article.errors.any? %>
      <div class="alert">
        <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>
     
        <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    
    <%= f.text_field :title, placeholder: 'Title' %><br />
    <%= f.cktext_area :content, class: 'ckeditor', placeholder: 'Content', rows: 15 %>
    
    <%= f.text_field :tag_list, placeholder: 'tags', value: @article.tag_list.join(", ") %>
    <div class="small"><em>Add up to 5 tags (separated by commas). Example: fitness, weight loss, marathon</em></div><br />
    
    <%= f.radio_button :status, 'published' %> 
    <%= label :status_published, 'Published' %>
    <%= f.radio_button :status, 'draft' %>
    <%= label :status_draft, 'Draft' %>
    <div class="small"><em>You can toggle article status directly from the article as well.</em></div><br />
    
          <fieldset>
            <legend>Image</legend>
              <br />
              <label class="upload-button">Upload Main Image<%= f.file_field :image, style: 'display:none;' %>
              </label>
              <div class="small"><br/><em>Image should be 620 pixels in width x 300 pixels in height. Must be less than 5Mb.</em></div>
          </fieldset>
    
    <%= f.submit %>
  <% end %>
</div>

